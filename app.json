{
  "name": "Ruby Sample",
  "description": "A barebones Ruby app using Sinatra",
  "repository": "https://github.com/heroku/ruby-sample",
  "logo": "https://www.ruby-lang.org/images/header-ruby-logo.png",
  "keywords": ["ruby", "sinatra"],
  "buildpacks": [
    {
      "url": "heroku-community/apt"
    },
    {
      "url": "heroku/ruby"
    }
  ],
  "environments": {
    "test": {
      "env": { "REDIS_VERSION": "7" },
      "addons": ["heroku-postgresql:in-dyno"],
      "scripts": {
      "test-setup": "echo $DATABASE_URL; psql $DATABASE_URL -c \"select * from pg_available_extensions();\"; psql $DATABASE_URL -c \"create extension vector;\"",
      "test": "1"
      }
  }
}
}

